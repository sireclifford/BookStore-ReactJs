import React from 'react';
import './Alert.css';

const Alert = () => (
  <div className="alert alert-warning" role="alert">
    You have no books to read. Add a book to get started.
  </div>
);

export default Alert;
